<h1>Products</h1>

<div id="products">
  <% @products.each do |product| %>
    <div>
    <!--
      <a href="/products/<%= product.id %>">
        <%= product.name %>
      </a>
    -->
    <!-- Easier way to do this -->
    <%= link_to product.name, product %>
    </div>
  <% end %>
</div>
